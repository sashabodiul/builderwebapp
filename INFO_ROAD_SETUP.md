# Настройка страницы InfoRoad для Telegram бота

## Описание

Страница `/inforoad/{tg_id}` предназначена для сбора информации о поездке от пользователя. Страница открывается из Telegram бота и не требует авторизации.

## URL структура

```
/inforoad/{telegram_id}?transport={transport}&time={time}
```

### Параметры:

- **`tg_id`** (обязательный) - Telegram ID пользователя из URL пути
- **`transport`** (опциональный) - Тип транспорта, передается через query параметр
- **`time`** (опциональный) - Время поездки, передается через query параметр

## Примеры URL

### Базовый URL (только telegram_id)
```
https://your-domain.com/inforoad/6315604689
```

### С параметрами от бота
```
https://your-domain.com/inforoad/6315604689?transport=Автомобиль&time=14:30
```

## Интеграция с Telegram ботом

### Python (python-telegram-bot)

```python
from telegram import InlineKeyboardButton, InlineKeyboardMarkup, WebAppInfo

# Создание кнопки для открытия веб-приложения
def create_info_road_button(telegram_id: int, transport: str = None, time: str = None):
    # Формируем URL
    url = f"https://your-domain.com/inforoad/{telegram_id}"
    
    # Добавляем query параметры, если они есть
    params = []
    if transport:
        params.append(f"transport={transport}")
    if time:
        params.append(f"time={time}")
    
    if params:
        url += "?" + "&".join(params)
    
    # Создаем кнопку с WebApp
    keyboard = [
        [InlineKeyboardButton(
            text="📝 Заполнить информацию о поездке",
            web_app=WebAppInfo(url=url)
        )]
    ]
    
    return InlineKeyboardMarkup(keyboard)

# Использование
def send_info_road_request(update, context):
    user_id = update.effective_user.id
    transport = "Автомобиль"  # Получаем из контекста бота
    time = "14:30"  # Получаем из контекста бота
    
    keyboard = create_info_road_button(user_id, transport, time)
    
    context.bot.send_message(
        chat_id=update.effective_chat.id,
        text="Пожалуйста, заполните информацию о поездке:",
        reply_markup=keyboard
    )
```

### Node.js (node-telegram-bot-api)

```javascript
const { InlineKeyboard } = require('node-telegram-bot-api');

function createInfoRoadButton(telegramId, transport = null, time = null) {
  let url = `https://your-domain.com/inforoad/${telegramId}`;
  
  const params = [];
  if (transport) params.push(`transport=${encodeURIComponent(transport)}`);
  if (time) params.push(`time=${encodeURIComponent(time)}`);
  
  if (params.length > 0) {
    url += '?' + params.join('&');
  }
  
  return InlineKeyboard.from({
    inline_keyboard: [[
      {
        text: '📝 Заполнить информацию о поездке',
        web_app: { url: url }
      }
    ]]
  });
}

// Использование
bot.on('message', (msg) => {
  const chatId = msg.chat.id;
  const userId = msg.from.id;
  const transport = 'Автомобиль'; // Из контекста бота
  const time = '14:30'; // Из контекста бота
  
  const keyboard = createInfoRoadButton(userId, transport, time);
  
  bot.sendMessage(chatId, 'Пожалуйста, заполните информацию о поездке:', {
    reply_markup: keyboard
  });
});
```

## Форма на странице

Страница содержит следующие поля:

1. **Причина поездки** (обязательное) - текстовое поле для ввода причины
2. **Транспорт** (только для отображения) - показывается, если передан через query параметр
3. **Время** (только для отображения) - показывается, если передан через query параметр
4. **Точка назначения на карте** (обязательное) - интерактивная карта для выбора места назначения

## Функциональность карты

- Клик по карте для выбора точки
- Перетаскивание маркера для точной настройки
- Кнопка "Моё местоположение" для автоматического определения текущей позиции
- Отображение координат выбранной точки

## Отправка данных

После заполнения формы данные отправляются на сервер (в данный момент логируются в консоль). 

Формат данных:
```json
{
  "telegram_id": 6315604689,
  "reason": "Причина поездки",
  "transport": "Автомобиль",
  "time": "14:30",
  "destination_latitude": 50.4501,
  "destination_longitude": 30.5234
}
```

## Настройка API endpoint

Для отправки данных на сервер нужно раскомментировать и настроить API запрос в файле `src/pages/InfoRoad/index.tsx`:

```typescript
// Раскомментировать и настроить:
await axios.post('/api/v1/inforoad', formData);
```

## Безопасность

⚠️ **Важно**: 
- Страница доступна без авторизации, но проверяет `telegram_id` из URL
- Рекомендуется добавить проверку на сервере, что запрос действительно пришел от указанного пользователя
- Используйте HTTPS для всех запросов

## Тестирование

Для локального тестирования:
1. Запустите приложение: `npm run dev`
2. Откройте в браузере: `http://localhost:5173/inforoad/6315604689?transport=Автомобиль&time=14:30`
3. Заполните форму и проверьте работу карты

